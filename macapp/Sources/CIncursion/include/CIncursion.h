/* See the Incursion LICENSE file for copyright information.

   Swift sees the engine through exactly one header. The include path walks
   back to the repo's copy so the two can never drift. */
#include "../../../../src/WmacBridge.h"
